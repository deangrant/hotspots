# Gate Contract Rule

Always-on reminder to run the hotspots verify gates and not relax thresholds
or tool pins to hide failures.

## When to use

Applied automatically (`alwaysApply: true`) for all sessions in this repo.

## Usage

Agents should follow the `.mdc` after Rust edits and when finishing an
implement-the-plan task. Full procedure:
[verify-gates](../../skills/verify-gates/SKILL.md).
